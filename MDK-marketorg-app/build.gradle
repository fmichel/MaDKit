plugins {
	id 'buildlogic.madkit-common-conventions'
}

application {
    mainClass = 'madkit.marketorg.Client'
    mainModule = 'madkit.marketorg'
}

tasks.withType(JavaExec) {
    jvmArgs '--add-modules', 'ALL-MODULE-PATH'
}

dependencies {
	api project(':MaDKit')
}